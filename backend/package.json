{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:up": "docker compose --env-file .env up -d",
    "db:down": "docker compose --env-file .env down",
    "db:reset": "docker compose --env-file .env down -v && docker compose --env-file .env up -d",
    "db:logs": "docker compose logs -f sqlserver",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.6.3",
    "mssql": "^12.5.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
